# Dependencies
node_modules/
.yarn/

# Build outputs
dist/
dist-ssr/

# Environment files — NEVER commit secrets
.env
.env.*
.env.local

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / Editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Legacy folder — local reference only, not tracked
legacy/

# Solana / Rust artifacts
target/
test-ledger/
.debugs/

# Agent / session scratch — local only
.brain/
HANDOFF.md

# Railway local tooling — NEVER track, NEVER push (reads tokens from AI Os .env)
.railway.sh
railway-api.mjs
.rwy.json
.railway/
